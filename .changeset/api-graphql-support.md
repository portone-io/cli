---
"@portone/cli": minor
---

Add `portone api graphql` with GraphQL variables, cursor pagination, `--slurp`, and response caching. Report GraphQL errors and exit with status 1 even when the HTTP status is 200.
