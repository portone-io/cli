---
"@portone/cli": minor
---

Add English and Korean support for help, prompts, authentication status, diagnostics, and login callback pages. Select the display language automatically from OS preferences, with overrides through `PORTONE_LANG` and the `language` setting in config.toml.
